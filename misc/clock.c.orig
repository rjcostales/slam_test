#include <time.h>
#include <stdio.h>

void main(void) {
    printf("%u\n", sizeof(clock_t));
    printf("%u\n", clock());
    for (int i = 0 ; i < 1024; i++)
        for (int j 0 ; j < 1024; j++)
            for ( int k; k < 1024 ; k++)
                int l = i + j / k;
    printf("%u\n", clock());
}

